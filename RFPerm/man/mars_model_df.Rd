% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generating_process_simulation.R
\name{mars_model_df}
\alias{mars_model_df}
\title{Generate MARS-like regression data (Uniform covariates) with optional nuisances}
\usage{
mars_model_df(n, p_nuisance, sigma)
}
\arguments{
\item{n}{Integer scalar. Number of observations (must be >= 1).}

\item{p_nuisance}{Integer scalar. Number of nuisance covariates (must be >= 0).}

\item{sigma}{Numeric scalar. Standard deviation of additive Gaussian noise (must be >= 0).}
}
\value{
A data.frame with columns:
\itemize{
  \item \code{Y1}: numeric, noise-free signal
  \item \code{X1..X_{5+p_nuisance}}: numeric covariates
  \item \code{Y}: numeric response
}
}
\description{
Generates a regression dataset with a nonlinear mean function and Gaussian noise.
The returned data frame contains the noise-free signal `Y1`, covariates `X*`, and
noisy response `Y`.
}
\examples{
df <- mars_model_df(n = 200, p_nuisance = 10, sigma = 1)
str(df)

}
