% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permrf.R
\name{permrf}
\alias{permrf}
\title{Random-forest permutation test using a train/validation split within df_train}
\usage{
permrf(df_train, df_test, B = 5000, tr_prop = 0.7)
}
\arguments{
\item{df_train}{Data frame with predictors and response \code{Y}.}

\item{df_test}{Data frame with predictors and response \code{Y}.}

\item{B}{Integer scalar; number of permutations.}

\item{tr_prop}{Numeric scalar in (0,1); training proportion within df_train.}
}
\value{
Numeric scalar p-value.
}
\description{
Random-forest permutation test using a train/validation split within df_train
}
\details{
INPUT CONTRACT:
\itemize{
  \item \code{df_train}, \code{df_test} are data.frames with response column \code{Y}.
  \item Predictors are columns \code{1:(ncol(df)-1)} and \code{Y} is the response.
  \item \code{tr_prop} is in (0,1) and controls the split within \code{df_train}.
}

Fits RF on a subset of df_train, compares validation squared errors vs test squared errors,
and returns a one-sided permutation p-value.
}
